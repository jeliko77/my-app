# 할일 추출기 설계서 (DESIGN)

**작성일:** 2026년 9월 17일
**근거 문서:** [PRD.md](PRD.md) → [PLAN.md](PLAN.md) → 이 문서

이 문서는 PLAN.md의 작업 23개를 **다 끝냈을 때의 최종 모습**을 적는다. 지금 코드와 다른 부분은 "PLAN n번"으로 어느 작업에서 바뀌는지 표시했다.

---

## 1. 화면 구성

페이지는 **하나**다. 위에서 아래로 한 줄로 쌓이고, 페이지 이동은 없다. 폭은 최대 576px로 가운데에 놓여, 휴대폰에서도 같은 모양으로 보인다.

```
                        할일 추출기                    ① 앱 이름 (작은 파란 글씨)

                     글 속 할 일만                     ① 큰 제목 (가운데, 굵게)
                     쏙 골라드려요

          회의 메모, 대화, 이메일을 그대로 붙여넣으세요.   ① 설명 (옅은 회색)
                  입력한 글은 저장되지 않아요.

╭──────────────────────────────────────────────────────╮
│ 여기에 글을 붙여넣으세요                              │ ② 입력 카드
│                                                      │
│                                                      │
│                                    1,234 / 5,000자   │ ③ 글자 수 (카드 안 오른쪽 아래)
╰──────────────────────────────────────────────────────╯
(                     할 일 뽑기                      ) ③ 파란 알약 버튼 (전체 폭)

╭──────────────────────────────────────────────────────╮
│  오류 문구 (문제가 있을 때만)                          │ ④ 오류 상자 (붉은 기운)
╰──────────────────────────────────────────────────────╯

╭──────────────────────────────────────────────────────╮
│  할 일 4개                           ( 전체 복사 )    │ ⑤ 결과 머리줄 (숫자만 파란색)
│                                                      │
│  ①  금요일까지 헤더 문구 세 가지 슬랙에 올리기  [복사] │ ⑥ 결과 목록 (동그라미 번호)
│      "내가 금요일까지 헤더 문구 세 가지 뽑아서…"       │    회색: 근거 원문 (PLAN 18)
│  ──────────────────────────────────────────────────  │    항목 사이 옅은 줄
│  ②  광고비 내역 받아 월간 보고서에 반영하기     [복사] │
│      "나는 그걸 받아서 월간 보고서에 반영해야 함"      │
│  …                                                   │
│                                                      │
│  ╭────────────────────────────────────────────────╮  │
│  │ 10개까지만 표시했습니다. 글을 나눠 넣어주세요.  │  │ ⑦ 잘림 안내 (노란 기운)
│  ╰────────────────────────────────────────────────╯  │
│        원문과 대조해 빠진 것이 없는지 확인해 주세요.   │ ⑧ 확인 안내 (가운데, 작게)
╰──────────────────────────────────────────────────────╯
```

| 번호 | 요소 | 언제 보이나 | 규칙 출처 | PLAN |
|---|---|---|---|---|
| ① | 앱 이름, 큰 제목, 설명 | 항상 | — | 완료 |
| ② | 입력 카드 | 항상 | PRD 4번 | 완료 |
| ③ | 글자 수 `현재 / 5,000자` (카드 안 오른쪽 아래) | 항상. 10자 미만·5,000자 초과면 앞에 안내 문구가 붙고, 초과면 빨간색 | PRD 5번 · nice | 완료 |
| ③ | [할 일 뽑기] 파란 알약 버튼 (카드 아래, 전체 폭) | 항상. 10자 미만·5,000자 초과·처리 중에는 흐리게 잠김. 처리 중에는 "뽑는 중..." | PRD 5번 | 완료 |
| ④ | 오류 상자 | 서버가 오류를 돌려줬을 때만 | — | 완료 |
| ⑤ | "할 일 N개" + [전체 복사] | 결과가 1개 이상일 때 | PRD must 2 | 완료 |
| ⑤ | "할 일이 없습니다" | 결과가 0개일 때 (⑥~⑧ 대신) | PRD 5번 | 완료 |
| ⑥ | 번호 목록 | 결과가 1개 이상일 때 | PRD must 2 | 완료 |
| ⑥ | 각 할 일 아래 회색 근거 원문 | 결과가 1개 이상일 때 | PRD nice | PLAN 18 |
| ⑥ | 항목별 [복사] | 결과가 1개 이상일 때 | PRD nice | PLAN 20 |
| ⑦ | 잘림 안내 | 할 일이 10개를 넘어 잘렸을 때만 | PRD 5번 | PLAN 12 |
| ⑧ | 원문 대조 안내 | 결과가 1개 이상일 때 | PRD 4번 | 완료 |

### 화면 상태

| 상태 | 버튼 | 결과 영역 |
|---|---|---|
| 처음 열었을 때 | 잠김 | 비어 있음 |
| 10자 미만 | 잠김 | 비어 있음, ③에 "10자 이상 입력해 주세요" |
| 5,000자 초과 | 잠김 | 비어 있음, ③이 빨간색 "글이 너무 깁니다" |
| 뽑는 중 | 잠김, "뽑는 중..." | 이전 결과를 지움 |
| 결과 1~10개 | 열림 | ⑤⑥⑧ |
| 결과 10개 초과 | 열림 | ⑤⑥⑦⑧ (10개만 표시) |
| 결과 0개 | 열림 | "할 일이 없습니다" |
| 오류 | 열림 | ④만. **입력한 글은 그대로 남아** 다시 누를 수 있음 |

전체 복사를 누르면 버튼 글자가 2초 동안 "복사됨"으로 바뀐다. 복사되는 내용은 **할 일 문장만 줄바꿈으로 이어 붙인 평문**이다. 번호와 근거 원문은 복사하지 않는다.

### 디자인 기준

토스뱅크 홈페이지(tossbank.com)의 **분위기만** 참고했다. 토스 로고·이름·그림은 쓰지 않는다.

| 항목 | 기준 |
|---|---|
| 화면 밝기 | **항상 어두운 화면.** 컴퓨터의 밝은/어두운 설정을 따르지 않는다 |
| 색 | 페이지 배경 `#101013`, 카드 `#1c1c21`, 구분선 `#2c2c33`, 강조 파란색 `#3182f6` (누르면 `#1b64da`) |
| 글자색 | 제목 흰색 `#ffffff`, 본문 `#e5e8eb`, 보조 설명 `#8b95a1` |
| 글꼴 | Pretendard (인터넷에서 불러오는 무료 글꼴. 끊기면 기본 글꼴로 보임) |
| 모양 | 카드는 크게 둥글게, 버튼은 알약 모양, 테두리 대신 배경색 차이로 구역을 나눈다 |
| 강조 | 파란색은 앱 이름, [할 일 뽑기] 버튼, 할 일 개수 숫자, 번호에만 쓴다 |
| 상태 색 | 오류는 붉은 기운, 잘림 안내는 노란 기운의 반투명 상자 |

색 값은 `src/app/globals.css` 한 곳에 이름을 붙여 모아 두었다. 색을 바꿀 때는 이 파일만 고친다.

---

## 2. 데이터 흐름

```
 [입력]                   [처리]                                     [출력]

 사용자가                 ── 브라우저 (page.tsx) ──
 글 붙여넣기  ───────▶  ❶ 글자 수 검사
                           10자 미만 / 5,000자 초과 → 버튼 잠금, 여기서 끝
                             │
                             │ POST /api/extract  { text }
                             ▼
                        ── Vercel 서버 (route.ts) ──
                        ❷ 글자 수 다시 검사 (화면을 우회한 호출 차단)
                           실패 → 400 오류 ──────────────────────▶ ④ 오류 상자
                             │
                        ❸ OpenAI에 보내기
                           지시문(규칙) + 글  ── OPENAI_API_KEY 사용
                           실패 → 502 오류 ──────────────────────▶ ④ 오류 상자
                             │
                             ▼
                        ── OpenAI (gpt-4o-mini) ──
                        ❹ 규칙대로 할 일을 전부 골라
                           정해진 모양의 JSON으로 답함
                             │
                             ▼
                        ── Vercel 서버 (route.ts) ──
                        ❺ 답 모양 검사 → 틀리면 502 오류
                        ❻ 중복 합치기
                        ❼ 10개 넘으면 자르고 truncated = true
                             │
                             │ { todos, truncated }
                             ▼
                        ── 브라우저 (page.tsx) ──
                        ❽ 화면 그리기 ───────────────────────────▶ ⑤⑥⑦⑧ 결과
                        ❾ [전체 복사] → 할 일 문장만 평문으로 ──────▶ 클립보드
```

**저장하는 곳은 없다.** 글과 결과는 요청 한 번이 끝나면 서버에 남지 않는다. 새로고침하면 화면에서도 사라진다. (PRD 6번 비범위)

### 규칙을 누가 지키나

| 단계 | 지키는 쪽 | 지키는 규칙 |
|---|---|---|
| ❶❷ | 코드 | 10자 이상, 5,000자 이하 |
| ❹ | AI 지시문 | 본인 할 일만, 주어 없으면 본인 일로 봄, 끝난 일 제외, 막연한 생각 포함, 40자 이내 `~하기`, 날짜·시간·마감 표현과 핵심 동작 남기기, 원문 순서, 같은 뜻 중복 합치기, 원문에 없는 내용 금지 |
| ❺ | 코드 | 답이 약속한 모양인지 |
| ❻❼ | 코드 | 중복 1개로, 최대 10개, 잘렸는지 표시, 할 일마다 근거 원문 찾아 붙이기 |
| ❽❾ | 코드 | 번호 목록, 0개면 "할 일이 없습니다", 평문 복사 |

**반드시 지켜야 하는 숫자 규칙은 AI에게 맡기지 않는다.** AI는 가끔 지시를 어기지만 코드는 어기지 않기 때문이다. 그래서 "최대 10개"도 지시문에서 빼고 코드(❼)로 옮긴다. (PLAN 10) 그래야 할 일이 더 있었는지 앱이 알 수 있다.

### 주고받는 데이터 모양

**요청** — 브라우저 → 서버

```json
{ "text": "붙여넣은 글 전체" }
```

**성공 응답** — 서버 → 브라우저 (최종 모양, PLAN 10·18 반영 후)

```json
{
  "todos": [
    {
      "text": "금요일까지 헤더 문구 세 가지 슬랙에 올리기",
      "source": "내가 금요일까지 헤더 문구 세 가지 뽑아서 슬랙에 올리기로 함."
    }
  ],
  "truncated": false
}
```

| 항목 | 뜻 |
|---|---|
| `todos[].text` | 할 일 문장. 화면 목록과 복사에 쓴다 |
| `todos[].source` | 그 할 일의 근거가 된 원문 문장. 화면에만 회색으로 보여주고 복사하지 않는다 |
| `truncated` | 10개를 넘어 잘렸으면 `true` → ⑦ 잘림 안내 |

> PLAN 10(`truncated`)과 PLAN 18(`{ text, source }`)까지 반영되어, 지금 코드가 이 모양으로 주고받는다.
>
> `source`는 **AI에게 받지 않고 앱이 직접 찾는다.** 할 일 문장과 글자가 가장 많이 겹치는 문장을 입력한 글에서 골라 붙이므로, 항상 실제 글 안의 문장이다. 겹치는 정도가 30%보다 낮으면 빈 문자열로 둔다. AI에게 원문까지 달라고 했을 때 할 일을 고르는 성적이 떨어졌기 때문이다(TEST-CASES.md 3차·4차 채점).

**오류 응답** — 모든 오류는 같은 모양이다.

```json
{ "error": "사용자에게 그대로 보여줄 안내 문구" }
```

| 상태 코드 | 언제 | 안내 문구 |
|---|---|---|
| 400 | 요청 형식이 JSON이 아님 | 요청 형식이 잘못되었습니다. |
| 400 | 10자 미만 | 글이 너무 짧습니다. 10자 이상 입력해 주세요. |
| 400 | 5,000자 초과 | 글이 너무 깁니다. 5,000자 이하로 나눠 넣어 주세요. |
| 500 | 서버에 `OPENAI_API_KEY`가 없음 | OPENAI_API_KEY가 설정되지 않았습니다. .env 파일을 확인해 주세요. |
| 502 | OpenAI 호출 실패 | AI 호출에 실패했습니다. 잠시 후 다시 시도해 주세요. |
| 502 | AI 답이 약속한 모양이 아님 | AI 응답을 읽지 못했습니다. 다시 시도해 주세요. |

---

## 3. 기술 선택

### 기본: Next.js (고정)

화면과 서버를 한 프로젝트에서 만들고, Part 5에서 **Vercel에 그대로 올린다.** Vercel은 Next.js를 만든 회사라 별도 설정 없이 배포된다. 서버 코드(`route.ts`)는 Vercel에서 요청이 올 때만 잠깐 켜지는 방식으로 돌아간다.

### 이미 쓰고 있는 것

| 기술 | 쉬운 설명 |
|---|---|
| **TypeScript** | 코드에 "이 값은 글자, 이 값은 숫자" 같은 표시를 붙여, 실행하기 전에 실수를 잡아주는 JavaScript입니다. |
| **Tailwind CSS** | `text-sm`, `rounded-lg` 같은 짧은 이름만 붙이면 글자 크기·모서리 모양 등이 바로 꾸며지는 디자인 도구입니다. |
| **OpenAI SDK** (`openai`) | OpenAI에 요청을 보내고 답을 받는 과정을 몇 줄로 줄여주는 공식 도우미 코드입니다. |
| **ESLint** | 코드를 실행하지 않고 읽기만 해서 흔한 실수를 찾아주는 맞춤법 검사기입니다. |
| **Pretendard** 글꼴 | 한글이 깔끔하게 보이는 무료 글꼴입니다. 설치 없이 인터넷에서 불러와 씁니다. |

### 추가 제안

| 기술 | 쉬운 설명 | 필요한 이유 | 언제 |
|---|---|---|---|
| **Zod** + OpenAI 구조화 출력 | AI에게 "답은 반드시 이 모양으로"라고 틀을 건네주고, 받은 답이 그 틀에 맞는지 자동으로 확인해 주는 검사기입니다. | 응답이 `{ text, source }` 모양으로 바뀌면 AI가 모양을 틀릴 곳이 늘어납니다. 지금은 손으로 짠 검사 코드가 이 일을 하는데, Zod를 쓰면 AI가 애초에 틀을 벗어나지 못합니다. 다른 패키지를 설치할 때 이미 함께 들어와 있어(4.6.5) `package.json`에 이름만 올리면 됩니다. | PLAN 10 또는 18 |
| **Vercel CLI** | 웹사이트 화면을 열지 않고 명령어 한 줄로 Vercel에 배포하게 해주는 도구입니다. | CLAUDE.md 규칙대로 `.env`의 `VERCEL_TOKEN`으로 배포와 환경변수 등록을 하려면 필요합니다. | PLAN 22 |

**제안하지 않은 것:** 데이터베이스, 로그인 라이브러리, 상태 관리 라이브러리(Redux 등)는 넣지 않는다. 저장과 로그인은 PRD 6번 비범위이고, 화면이 하나뿐이라 React 기본 기능(`useState`)으로 충분하다. 채점(PLAN 15·16)도 새 도구 없이 할 수 있다. 이미 설치된 Node.js로 TEST-CASES.md의 글을 API에 차례로 보내는 짧은 스크립트면 된다.

---

## 4. 파일 구성

| 파일 | 역할 | 바뀌는 작업 |
|---|---|---|
| `src/app/page.tsx` | 화면 전체. 입력, 버튼, 결과, 복사 | PLAN 12·18·20 |
| `src/app/api/extract/route.ts` | 서버. 검사 → OpenAI 호출 → 후처리 | PLAN 9·10·18 |
| `src/lib/limits.ts` | 10자·5,000자 기준. 화면과 서버가 함께 씀 | 변경 없음 |
| `src/lib/source.ts` | 할 일마다 근거 원문 문장을 입력한 글에서 찾는다 | PLAN 18 |
| `src/lib/types.ts` **(새 파일)** | 성공·오류 응답 모양을 한 번만 적어두고 화면과 서버가 함께 씀 | PLAN 10 |
| `src/app/layout.tsx` | 탭 제목, 언어(`ko`), Pretendard 글꼴 불러오기 | 변경 없음 |
| `src/app/globals.css` | 색과 글꼴 기준 (1장 "디자인 기준") | 변경 없음 |

`types.ts`를 새로 두는 이유는 `limits.ts`와 같다. 응답 모양이 두 번(PLAN 10, 18) 바뀌는데, 화면과 서버에 따로 적혀 있으면 한쪽만 고쳐 어긋나기 쉽다.

---

## 5. 검증 계획

| 무엇을 | 어떻게 | PLAN |
|---|---|---|
| 코드 오류 | `npm run lint`, `npm run build` — 작업마다 (CLAUDE.md 검증 루프) | 매 작업 |
| 서버 규칙 | 2장 오류 표의 6가지 경우를 요청으로 보내 상태 코드와 문구 확인 | 10·18 이후 |
| 화면 상태 | 1장 "화면 상태" 표의 8가지를 브라우저에서 직접 확인 | 12·18·20 이후 |
| AI 규칙 | TEST-CASES.md 8~10번(규칙), 1~7번(채점) | 15·16·21 |
| 배포 | 배포 주소에서 테스트 글 1개 이상 | 23 |
