# 할일 추출기 개발 계획 (PLAN)

**작성일:** 2026년 9월 17일
**근거 문서:** [PRD.md](PRD.md) · [TEST-CASES.md](TEST-CASES.md)

## 이번 사이클 목표

PRD 5번의 must 기능 2개를 **PRD 규칙 그대로** 완성하고, 테스트 글로 목표 수치를 확인한 뒤 **Vercel에 배포**한다. nice 기능은 must 검증이 끝난 뒤 이어서 만든다.

## 성공 기준

| # | 기준 | 확인 방법 |
|---|---|---|
| 1 | **누락** — 측정용 글 7개의 정답 25개 중 **23개 이상** 뽑아냄 (PRD 목표 1) | TEST-CASES.md 1~7번 채점표 |
| 2 | **지어낸 항목 0개** (PRD 목표 2) | TEST-CASES.md 1~7번 채점표 |
| 3 | **규칙 검증 3개 모두 기대대로 동작** — 할 일 없음 / 10개 초과 안내 / 10자 미만 차단 | TEST-CASES.md 8~10번 |
| 4 | **붙여넣고 결과를 확인하기까지 30초 이내** (PRD 목표 3) | 측정용 글로 직접 시간 재기 |
| 5 | **`npm run lint`·`npm run build` 오류 0개** | CLAUDE.md 검증 루프 |
| 6 | **배포 주소에서도 1~5가 똑같이 동작** | Vercel 주소에 테스트 글 넣어 확인 |

## 작업 순서

`[x]` 끝남 · `[ ]` 남음 — 먼저 해두면 뒤 작업이 쉬워지는 순서로 정렬했다.

1. [x] 프로젝트 준비 — Next.js 설치, `.env`에 `OPENAI_API_KEY`, `.gitignore`에 `.env`·`node_modules` 등록
2. [x] 글자 수 기준 한 곳에 두기 — `src/lib/limits.ts`에 10자·5,000자를 두어 화면과 서버가 같은 값을 쓰게 하기
3. [x] API 요청·응답 형식 정하기 — `/api/extract`가 `{ text }`를 받고 `{ todos }` 또는 `{ error }`를 돌려주기
4. [x] 화면 뼈대 — 입력칸, [할 일 뽑기] 버튼, 결과 영역을 만들고 API와 연결하기
5. [x] 입력 길이 검사 — 10자 미만·5,000자 초과면 화면은 버튼을 잠그고 서버는 AI 호출 없이 막기
6. [x] AI 호출 — OpenAI를 JSON 모드로 불러 할 일 목록 받기
7. [x] 오류 처리 — AI 호출·응답 해석이 실패하면 안내 문구를 띄우고 입력한 글은 지우지 않기
8. [x] 기본 추출 지시문 — 본인 할 일만, 끝난 일 제외, 막연한 생각 포함, 40자 이내 `~하기`, 원문 순서 유지
9. [x] 주어 없는 할 일 지시문 추가 — 누구 일인지 모르는 할 일은 본인 할 일로 보고 뽑게 하기
10. [x] 개수 제한을 코드로 옮기기 — 지시문의 "최대 10개"를 빼고 AI에게서 전부 받아, 코드가 10개로 자르고 잘렸는지(`truncated`)를 함께 돌려주기
11. [x] 중복 합치기 — 같은 할 일은 코드에서 1개로 합친 뒤 10개 자르기를 하기
12. [x] 10개 초과 안내 — `truncated`이면 결과 아래에 "10개까지만 표시했습니다. 글을 나눠 넣어주세요" 띄우기
13. [x] 결과 표시 — 번호 목록으로 보여주고 0개면 "할 일이 없습니다" 띄우기
14. [x] 전체 복사 — 번호 없이 줄바꿈으로만 구분한 평문으로 복사하기
15. [x] 규칙 검증 — TEST-CASES.md 8·9·10번을 넣어 기대 동작 3개 확인하기 (성공 기준 3)
16. [x] 정답률 채점 — TEST-CASES.md 1~7번을 넣어 채점표 채우기 (성공 기준 1·2·4)
17. [x] 기준 미달이면 고치고 다시 재기 — 틀린 유형을 보고 지시문을 고친 뒤 15·16번 반복하기
18. [x] nice: 근거 원문 문장 보여주기 — 응답을 `{ todo, source }` 형태로 넓히고 각 할 일 옆에 원문 문장 표시하기
19. [x] nice: 입력칸 글자 수 표시
20. [x] nice: 항목별 개별 복사 버튼
21. [x] 18번 이후 재채점 — 응답 형식이 바뀌었으므로 15·16번을 한 번 더 돌려 성공 기준 유지 확인하기
22. [x] Vercel 배포 — Vercel 환경변수에 `OPENAI_API_KEY`를 등록하고 `.env`의 `VERCEL_TOKEN`으로 배포하기
23. [x] 배포 주소 확인 — 배포된 주소에서 테스트 글을 넣어 성공 기준 6 확인하기
