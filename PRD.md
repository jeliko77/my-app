# 할일 추출기 서비스 기획서 (PRD)

**작성자:** 정완준
**작성일:** 2026년 9월 16일

## 1. 추진 배경 (문제 정의)

*누가, 어떤 업무에서, 어떤 불편을 겪는지 적는 칸입니다.*

개인 사용자(정완준)는 메모, 대화, 이메일 등 형식을 가리지 않고 **줄글 안에 할 일이 섞여 있는 글**을 수시로 다룬다. 할 일은 분명 그 안에 적혀 있지만 설명과 배경 사이에 흩어져 있어, 눈으로 훑는 것만으로는 전부 걸러내기 어렵다.

그래서 생기는 진짜 문제는 "찾기 귀찮다"가 아니라 **"빠뜨린다"**는 것이다. 급하게 읽고 넘어간 글에서 할 일 하나를 놓치면 그 사실조차 나중에야 알게 되고, 이미 처리 시점을 놓친 뒤인 경우가 많다.


## 2. 현행 방식과 한계

*지금은 이 일을 어떻게 처리하고 있고, 무엇이 아쉬운지 적는 칸입니다.*

현재는 별도의 도구 없이 **사람이 직접 읽고 손으로 옮겨 적는 방식**으로 처리하고 있다. 글을 처음부터 끝까지 읽으며 할 일로 보이는 문장을 눈으로 골라내고, 그것을 메모장이나 할 일 앱에 다시 입력한다.

이 방식의 한계는 세 가지다.

- **읽는 사람의 집중도에 결과가 좌우된다.** 같은 글이라도 급하게 읽으면 빠뜨리고, 천천히 읽으면 걸러낸다. 결과가 일정하지 않다.
- **빠뜨렸는지 확인할 방법이 없다.** 옮겨 적은 목록만 남고 원문과 대조하지 않으므로, 누락은 한참 뒤 문제가 터진 후에야 드러난다.
- **글이 길수록 급격히 나빠진다.** 짧은 메모는 괜찮지만 긴 회의 메모나 여러 주제가 섞인 글에서는 뒷부분일수록 놓치기 쉽다.



## 3. 목표와 기대효과 (성공 기준)

*측정 가능한 숫자로 적는 칸입니다 — 처리 시간, 문의 건수, 정답률 등.*

핵심 문제가 "빠뜨린다"이므로, 첫 번째 지표를 **누락률**로 잡는다.

**목표 1 — 누락률 (핵심 지표)**
직접 만든 테스트용 글 **10개**로 잰다. 글과 정답, 채점표는 [TEST-CASES.md](TEST-CASES.md)에 있다.

- **측정용 7개** — 정답은 모두 **25개**다. 이 중 **23개 이상(92%)**을 빠뜨리지 않고 뽑아내면 목표 달성이다.
- **규칙 검증용 3개** — 점수에는 넣지 않고, 5번 규칙(할 일 없음, 10개 초과, 10자 미만)대로 동작하는지만 본다.

**목표 2 — 지어낸 항목 0개**
뽑아낸 할 일 중 **원문에 없는 내용이 0개**여야 한다. 빠뜨리는 것만큼이나, 없는 일을 만들어내면 결과 전체를 믿을 수 없게 된다.

**목표 3 — 처리 시간**
글 하나를 읽고 할 일을 옮겨 적는 데 걸리던 **3~5분**을, 붙여넣고 결과를 확인하는 **30초 이내**로 줄인다.



## 4. 사용자와 이용 흐름

*누가 쓰는지와 사용 순서를 화살표로 적는 칸입니다.*

**사용자:** 정완준 1인. 로그인이나 계정 구분이 필요 없는 개인용 도구다.

**이용 흐름:**

글 복사 → 화면의 입력칸에 붙여넣기 → [할 일 뽑기] 버튼 클릭 → AI가 문장을 읽고 할 일만 추려냄 → 결과가 목록으로 화면에 표시됨 → 사용자가 원문과 눈으로 대조해 확인 → [전체 복사] 버튼으로 복사해 평소 쓰는 곳에 붙여넣기

흐름은 **한 화면에서 시작해 한 화면에서 끝난다.** 페이지 이동이 없고, 결과를 앱 안에 저장하지도 않는다.



## 5. 주요 기능 (must / nice)

*must는 딱 2개까지만, 기능마다 "AI가 지킬 규칙"을 함께 적는 칸입니다.*

### must

#### must 1 — 글에서 할 일 뽑아내기

AI가 지킬 규칙:

- 원문에 **실제로 적힌 내용만** 근거로 삼는다. 추측하거나 보충해서 **지어낸 항목은 0개**여야 한다.
- **글쓴이 본인이 할 일만** 뽑는다. 다른 사람이 하기로 한 일은 제외한다.
- **주어가 없어 누구 일인지 알 수 없는 할 일은 본인 할 일로 보고 뽑는다.** (예: "보고서 금요일까지 제출할 것") 빠뜨리는 것보다 하나 더 뽑히는 편이 낫다.
- **이미 끝난 일은 제외**한다. 앞으로 해야 할 일만 뽑는다.
- 마감이나 구체성이 없는 **막연한 생각·다짐도 할 일로 뽑는다.** (예: "운동하기", "빵집 가보기")
- 한 번에 **최대 10개**까지만 뽑는다. 그보다 많으면 앞의 10개만 내고, 결과 아래에 **"10개까지만 표시했습니다. 글을 나눠 넣어주세요"**라고 안내한다. 잘린 사실을 알리지 않으면 이 앱이 막으려는 "빠뜨림"이 다시 생기기 때문이다.
- 각 항목은 **40자 이내 한 문장**으로, `~하기` 형태로 끝낸다. (예: "보고서 초안 금요일까지 보내기")
- 원문의 **날짜·시간·마감 표현과 핵심 동작은 할 일 문장에 남긴다.** (예: "오늘 저녁", "오후 6시 전에", "전화해서 예약 잡기") 따로 떼어 칸으로 만들지는 않는다.
- 원문 **등장 순서를 그대로 유지**한다. 중요도로 재정렬하지 않는다.
- 같은 내용이 두 번 나오면 **1개로 합친다.**
- 할 일이 하나도 없으면 빈 목록 대신 **"할 일이 없습니다"**를 표시한다.
- 입력이 **10자 미만**이면 AI를 호출하지 않고 안내만 띄운다.
- 입력이 **5,000자를 넘으면** AI를 호출하지 않고 글을 나눠 넣으라는 안내만 띄운다. 긴 회의록 한 편은 충분히 들어가는 길이이며, 요금과 응답 시간이 커지는 것을 막는다.

#### must 2 — 결과 목록 표시와 전체 복사

AI가 지킬 규칙:

- 결과는 **번호 붙은 목록**으로만 보여준다. 설명, 요약, 인사말을 덧붙이지 않는다.
- [전체 복사]는 **줄바꿈으로 구분된 평문**만 복사한다. 번호나 기호는 빼서, 어느 앱에 붙여넣어도 깨지지 않게 한다.

### nice

여유가 되면 추가한다.

- 각 할 일 옆에 **근거가 된 원문 문장** 함께 보여주기 — 대조 확인이 빨라진다
- 입력칸 **글자 수 표시**
- 항목별 **개별 복사** 버튼



## 6. 범위와 비범위

*이번 실습에서 만들 것과, 절대 만들지 않을 것을 나눠 적는 칸입니다.*

### 범위 (만들 것)

- 한 화면짜리 웹 페이지 — 입력칸, [할 일 뽑기] 버튼, 결과 목록, [전체 복사] 버튼
- 서버 쪽 API 1개 — 글을 받아 AI를 호출하고 할 일 목록만 돌려줌
- 테스트용 글 10개(측정용 7개 + 규칙 검증용 3개)와 정답·채점표 — [TEST-CASES.md](TEST-CASES.md)
- Vercel 배포

### 비범위 (만들지 않을 것)

- **로그인, 회원가입, 계정** — 혼자 쓰는 도구라 필요 없다
- **저장 기능** — 데이터베이스를 두지 않는다. 새로고침하면 결과는 사라진다
- **할 일 관리 기능** — 체크, 수정, 삭제, 마감일 설정. 그건 평소 쓰던 할 일 앱의 몫이고, 이 앱은 **뽑아서 넘겨주는 데서 끝난다**
- **파일 업로드** — PDF, 워드, 이미지, 음성. 붙여넣기만 받는다
- **마감일·담당자 자동 인식** — 할 일 문장만 뽑는다
- **다른 서비스 연동** — 노션, 구글 캘린더, 슬랙
- **모바일 앱, 다국어 지원**



## 7. 보안·개인정보 검토

*사용 문서의 공개등급, 개인정보 여부, API 키 관리 방법을 적는 칸입니다.*

**① 사용 문서의 공개등급**

개인 메모뿐 아니라 **회사 업무 문서(회의록, 업무 메일, 기획 메모 등)도 입력 대상에 포함**한다.

단, 입력한 글은 외부 AI 서비스로 전송되므로 **사용 전 회사의 외부 AI 도구 사용 규정을 확인한다.** 규정상 반출이 금지된 등급의 문서는 제외한다.

**② 개인정보 여부**

회사 문서에는 **동료·고객의 이름, 연락처, 거래 내역**이 섞일 가능성이 개인 메모보다 훨씬 높다. 앱은 아무것도 저장하지 않지만(6번 비범위) AI로 전송되는 것은 사실이므로 다음을 원칙으로 한다.

- **주민등록번호, 계좌·카드번호, 비밀번호는 절대 넣지 않는다.**
- **고객 개인정보**(이름+연락처 조합 등)는 지우거나 가명으로 바꾼 뒤 넣는다.
- 사내 동료 이름 정도는 허용하되, 필요 없으면 지우고 넣는다.

**③ API 키 관리**

사용 서비스는 **OpenAI**이며, 키는 `OPENAI_API_KEY` 이름으로 관리한다.

- 키는 **`.env` 파일에만** 둔다. 코드 안에 직접 적지 않는다.
- `.env`는 `.gitignore`에 등록돼 있어 **GitHub에 올라가지 않는다.** (STEP 3에서 확인 완료)
- 키는 **서버 쪽 코드에서만** 사용한다. `NEXT_PUBLIC_` 접두사를 **붙이지 않는다** — 붙이면 브라우저로 전달되어 누구나 볼 수 있다.
- Vercel 배포 시에는 `.env`가 올라가지 않으므로, **Vercel 설정 화면의 환경변수에 따로 등록**한다.
- 키가 노출된 것 같으면 **즉시 폐기하고 새로 발급받는다.**

**④ 배포 주소 관리**

Vercel에 배포하면 **주소를 아는 사람은 누구나 접속**할 수 있다(로그인은 6번에서 비범위). 회사 문서를 다루는 도구인 만큼 **배포 주소를 외부에 공유하지 않는다.**



## 8. 기술 스택

*개발에 사용할 도구 모음을 적는 칸입니다 (이 과정에서는 Next.js로 고정).*

**Next.js** — 화면(프론트)과 서버 쪽 코드(API)를 한 프로젝트 안에서 함께 처리한다. 사용자가 보는 입력 화면도, AI를 호출하는 서버 코드도 모두 이 안에서 만든다.

**고정 이유**

- STEP 2에서 `my-app` 폴더에 **이미 설치를 마쳤다.**
- Part 5에서 **Vercel로 배포할 예정**이라, 설치한 것을 그대로 이어서 쓰기 위함이다.

**함께 쓰는 것**

- **OpenAI API** — 할 일을 뽑아내는 AI 호출용 (7번에서 확정)
- **Vercel** — 배포처 (6번 범위에 포함)


## 9. 개발 단위

*핵심 기능(must 1·2)을 한 번에 하나씩 만들고 확인할 수 있는 크기로 나눈 목록입니다. 위에서부터 순서대로 만듭니다.*

`[x]` 코드 작성 완료 · `[ ]` 아직 안 함

- [x] 1. 프로젝트 준비 — Next.js 설치, `.env`에 `OPENAI_API_KEY` 넣기, `.gitignore`에 `.env` 등록 확인
- [x] 2. 화면 뼈대 — 입력칸 하나와 [할 일 뽑기] 버튼만 있는 한 화면 만들기
- [x] 3. API 뼈대 — `/api/extract`가 글을 받아 응답을 돌려주는지 연결만 확인하기
- [x] 4. 짧은 입력 차단 — 10자 미만이면 화면에서 버튼을 잠그고, 서버에서도 AI 호출 없이 막기
- [x] 5. 긴 입력 차단 — 5,000자 초과면 화면에서 버튼을 잠그고, 서버에서도 AI 호출 없이 안내 문구 돌려주기
- [x] 6. AI 호출 — OpenAI에 글을 보내고 할 일 목록을 JSON으로 받아오기
- [x] 7. 추출 규칙 지시문 — 본인 할 일만, 끝난 일 제외, 막연한 생각 포함, 40자 이내 `~하기`, 원문 순서 유지
- [x] 8. 주어 없는 할 일 규칙 — 누구 일인지 알 수 없는 할 일은 본인 할 일로 보고 뽑도록 지시문에 추가하기
- [x] 9. 응답 오류 처리 — AI 호출 실패나 JSON 해석 실패 시 안내 문구를 띄우고 입력한 글은 그대로 두기
- [x] 10. 중복 합치기 — 같은 할 일이 두 번 오면 코드에서 1개로 합치기
- [x] 11. 10개 초과 안내 — AI에게서는 전부 받고, 코드에서 10개로 자른 뒤 잘렸으면 "10개까지만 표시했습니다. 글을 나눠 넣어주세요" 띄우기
- [x] 12. 결과 표시 — 번호 붙은 목록으로 보여주고, 0개면 "할 일이 없습니다" 띄우기
- [x] 13. 전체 복사 — 번호 없이 줄바꿈으로만 구분된 평문으로 클립보드에 복사하기
- [x] 14. 규칙 검증 — TEST-CASES.md 8·9·10번 글로 할 일 없음, 10개 초과, 10자 미만 동작 확인하기
- [x] 15. 정답률 채점 — TEST-CASES.md 측정용 7개 글로 25개 중 23개 이상, 지어낸 항목 0개 확인하기
- [x] 16. 배포 — Vercel 환경변수에 `OPENAI_API_KEY` 등록하고 배포하기
